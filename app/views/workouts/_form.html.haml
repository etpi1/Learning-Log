= simple_form_for(@workout, html: { class: 'form-horizontal' }) do |f|
	= f.input :date, label: "Date"
	= f.input :workout, label: "Please input your name:", input_html: { class: "form-control" }
	= f.input :mood, label: "What are you working on?", input_html: { class: "form-control" }
	= f.input :length, label: "Progress log / Notes", input_html: { class: "form-control" }
	%br/
	= f.button :submit, value: "Step up now"